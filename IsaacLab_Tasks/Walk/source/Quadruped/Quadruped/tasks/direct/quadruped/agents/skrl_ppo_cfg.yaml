seed: 42


# Models are instantiated using skrl's model instantiator utility
# https://skrl.readthedocs.io/en/latest/api/utils/model_instantiators.html
models:
  separate: False
  policy:  # see gaussian_model parameters
    class: GaussianMixin
    clip_actions: False
    clip_log_std: True
    min_log_std: -20.0
    max_log_std: 2.0
    initial_log_std: 0.0
    network:
      - name: net
        input: OBSERVATIONS
        layers: [512, 256, 128]
        activations: elu
    output: ACTIONS
  value:  # see deterministic_model parameters
    class: DeterministicMixin
    clip_actions: False
    network:
      - name: net
        input: OBSERVATIONS
        layers: [512, 256, 128]
        activations: elu
    output: ONE


# Rollout memory
# https://skrl.readthedocs.io/en/latest/api/memories/random.html
memory:
  class: RandomMemory
  memory_size: -1  # automatically determined (same as agent:rollouts)


# PPO agent configuration (field names are from PPO_DEFAULT_CONFIG)
# https://skrl.readthedocs.io/en/latest/api/agents/ppo.html
agent:
  class: PPO
  rollouts: 32
  learning_epochs: 8
  mini_batches: 8
  discount_factor: 0.99
  lambda: 0.95
  learning_rate: 5.0e-04
  learning_rate_scheduler: KLAdaptiveLR
  learning_rate_scheduler_kwargs:
    kl_threshold: 0.03
  state_preprocessor: RunningStandardScaler
  state_preprocessor_kwargs: null
  value_preprocessor: RunningStandardScaler
  value_preprocessor_kwargs: null
  random_timesteps: 0
  learning_starts: 0
  grad_norm_clip: 1.0
  ratio_clip: 0.2
  value_clip: 0.2
  clip_predicted_values: True
  # Kept at 0.0. 0.005 was tried to stop the action std collapsing during fine-tuning
  # and did stop it, but the entropy gradient on log_std is a constant push with
  # nothing balancing it, so std grew EXPONENTIALLY - a steady ~3% per logged point,
  # 0.073 -> 0.118 in 31 min with no sign of settling. The noise-sensitive penalties
  # (action_rate_l2, dof_acc_l2, ang_vel_xy_l2) and track_lin_vel_xy_exp all degraded
  # with it, for ~6% gain on the foot-height objective it was meant to serve.
  entropy_loss_scale: 0.0
  value_loss_scale: 2.0
  kl_threshold: 0.0
  rewards_shaper_scale: 0.1
  time_limit_bootstrap: False
  # logging and checkpoint
  experiment:
    directory: "quadruped_direct"
    experiment_name: ""
    # Fixed, not `auto`. skrl's auto sets write_interval = timesteps/100, so the plotted value
    # is a mean over a window whose width depends on max_timesteps -- a 500k run and a 1M run
    # average over different windows and their early curves are not comparable.
    write_interval: 1000
    # Also fixed: auto is timesteps/10, so best_agent gets chosen from a different candidate
    # set depending on the budget (10 snapshots for 500k, 10 for 1M but at different steps).
    checkpoint_interval: 10000


# Sequential trainer
# https://skrl.readthedocs.io/en/latest/api/trainers/sequential.html
trainer:
  class: SequentialTrainer
  timesteps: 300000
  environment_info: log